# Configuration

Runtime configuration constants.

## Retries

`MAX_RETRIES` controls how many times a failed request is retried. The default is 3.

## Timeout

`TIMEOUT_SECONDS` sets the request timeout in seconds. The default is 30.
