{
  "project": "DocGuard — Self-Healing Technical Documentation",
  "root": "D:/self-healing-docs",
  "current_phase": 6,
  "active_milestone": "COMPLETE (+ real-world evaluation done)",
  "completion_pct": 95,
  "features_total": 22,
  "features_passed": 21,
  "features_in_progress": 0,
  "features_blocked": 0,
  "features_partial": 1,
  "features_planned": 0,
  "integration_branch": "integration",
  "phases_passed": [0, 1, 2, 3, 4, 5, 6],
  "real_world_eval": {
    "repos": ["pydantic", "fastapi"],
    "evidence": "docs/eval/*.json + README 'Real-world evaluation'",
    "findings": "Zero false positives on real repos. Mock = high-precision structural baseline, LOW recall. Hand-verified Pydantic Field(frozen=) rename: mock flags stale + surgical fix; real DeepSeek LLM flags stale (correct reasoning) but repair returned no change.",
    "real_llm": "OpenAI-compatible NVIDIA endpoint wired via .env (DOCGUARD_OPENAI_BASE_URL, model deepseek-ai/deepseek-v4-flash). Works; slow ~30-70s/call."
  },
  "open_followups": [
    "DONE (offline) session-3: real-LLM repair now feeds the NEW code + stale_claims + scope into the repair prompt (root cause: model never saw what to repair toward, so it echoed), adds ASD-STE100 style constraints for minimal diffs, salvages malformed JSON, and fills unified_diff. Contract covered by H-04..H-07. STILL TO CONFIRM: one live DeepSeek run to verify the endpoint now returns an applied change (needs .env key, ~30-70s/call).",
    "DONE (offline) session-3: code->doc mapping precision. _exact now requires a documentation signal (symbol in heading, repeated, defined/called, or short focused blurb) instead of any lone inline mention -> kills tutorial over-linking. Knob: _SHORT_SECTION in mapper.py. Covered by H-01..H-03.",
    "OPTIONAL: faster LLM endpoint for CI; real GitHub Action E2E on a live repo (BLK-GH); re-run scripts/eval_realrepo.py to measure the precision gain on pydantic/fastapi."
  ],
  "next_action": "Confirm real-LLM repair end-to-end with one live DeepSeek run (DOCGUARD_LLM_PROVIDER=openai, key in .env), then re-run eval_realrepo.py to quantify the mapping-precision gain. Core + offline hardening COMPLETE; 96 tests green, ruff+mypy clean.",
  "updated": "build-session-3"
}
